{
  "name": "fruitsshop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "rm -rfv dist/ && parcel build src/index.html && node rewriteURLs.js",
    "build-and-deploy": "rm -r -f -v dist/ && parcel build src/index.html && node rewriteURLs.js && ./deploy.sh"
  },
  "keywords": [],
  "author": "Tomas Töws",
  "license": "No license",
  "devDependencies": {
    "autoprefixer": "^9.4.10",
    "parcel": "^2.4.0",
    "replace-in-file": "^3.3.2",
    "sass": "^1.17.2"
  }
}
